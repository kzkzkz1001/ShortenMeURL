# 🚀 URL Shortener Service

## ✨ Key Features
* **Efficient Shortening**: Converts long URLs into unique 6-character short codes.
* **High-Performance Redirection**:
    * **Caching Strategy**: Integrates Redis as a caching layer.
    * **Cache-Aside Pattern**: Implements automatic cache population.

## 🛠 Tech Stack
* **Framework**: Flask
* **Database**: MySQL
* **Cache**: Redis

## ⚙️ Getting Started
### Environment Setup
```bash
pip install flask flask-sqlalchemy pymysql redis

### Steps to update:
1.  **Edit the file on GitHub**: Click the "pencil" icon (Edit this file) on the top right of your `README.md` page in your browser.
2.  **Add the syntax**: Use the format above to add `#`, `##`, `**`, and backticks (```) for code blocks.
3.  **Preview**: Click the **Preview** tab (which you can see in your screenshot) to ensure it looks the way you want.
4.  **Commit**: Scroll down and click **Commit changes**.
